# docs/

This directory exists as a placeholder for GitHub Pages / Jekyll site files.

Move your site files (for example: `index.md` or `index.html`, `_config.yml`, `assets/`, `_layouts/`, `_includes/`, etc.) into this `docs/` directory so the Pages build can find them at `/github/workspace/docs`.

Once moved, commit and push; the Pages workflow should then succeed.
